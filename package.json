{
  "name": "consult",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node inject_build && vite",
    "build": "node inject_build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.0.0"
  }
}
